langgraph
numpy>=1.24
pandas>=2.0
scikit-learn>=1.4
# Pin below 3.x to avoid pulling optional CUDA stack on some pip installs (use GPU build if needed).
xgboost>=2.0,<2.1
catboost
fastapi
uvicorn[standard]
pytest
httpx
matplotlib
scipy
